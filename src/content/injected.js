/**
 * Placeholder — reserved for the injected page-context script.
 * This file runs in the PAGE world (not the isolated content script world).
 * Useful for intercepting window-level APIs (fetch, XHR) in future iterations.
 */
console.log('[Testocan] Injected script placeholder loaded.');
